import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.visuallab.app',
  appName: 'AI Visual Lab',
  webDir: 'dist',
  backgroundColor: '#03040a',
}

export default config
